@page
@model TechSupportRagBot.Pages.ProfileModel
@{
    ViewData["Title"] = TechSupportRagBot.Services.UiText.T(HttpContext, "Profile");
}

<section class="page-head">
    <div>
        <p class="eyebrow">@TechSupportRagBot.Services.UiText.T(HttpContext, "Account")</p>
        <h1>@TechSupportRagBot.Services.UiText.T(HttpContext, "Profile")</h1>
    </div>
    <a class="btn-main as-link" asp-page="/ProfileEdit">@TechSupportRagBot.Services.UiText.T(HttpContext, "EditProfile")</a>
</section>

<section class="panel narrow profile-card">
    <div class="profile-head">
        @if (!string.IsNullOrWhiteSpace(Model.CurrentAvatarPath))
        {
            <img class="avatar avatar-large" src="/@Model.CurrentAvatarPath.Replace("\\", "/")" alt="avatar" />
        }
        else
        {
            <div class="avatar avatar-large">@Model.Initials</div>
        }
        <div>
            <h2>@Model.FullName</h2>
            <p>@Model.UserName</p>
        </div>
    </div>

    <dl class="profile-details">
        <div><dt>Email</dt><dd>@(Model.Email ?? "—")</dd></div>
        <div><dt>@TechSupportRagBot.Services.UiText.T(HttpContext, "Position")</dt><dd>@(Model.Position ?? "—")</dd></div>
        <div><dt>@TechSupportRagBot.Services.UiText.T(HttpContext, "Language")</dt><dd>@(Model.Language ?? "—")</dd></div>
        <div><dt>@TechSupportRagBot.Services.UiText.T(HttpContext, "Gender")</dt><dd>@(Model.Gender ?? "—")</dd></div>
        <div><dt>@TechSupportRagBot.Services.UiText.T(HttpContext, "AutoTranslate")</dt><dd>@TechSupportRagBot.Services.UiText.T(HttpContext, Model.AutoTranslateMessages ? "Enabled" : "Disabled")</dd></div>
    </dl>
</section>
